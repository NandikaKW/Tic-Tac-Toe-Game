package com.assignment.tictactoe.service;

// Defines the Piece enum representing the possible values on the Tic-Tac-Toe board
public enum Piece {
    X,      // Represents the first player's piece (X)
    O,      // Represents the second player's piece (O)
    EMPTY;  // Represents an empty space on the board
}
